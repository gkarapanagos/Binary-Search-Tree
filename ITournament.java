
public interface ITournament {
	boolean isBalanced(); //checks whether the tournament's left and right branches are balanced
	int count();	// counts the sub-branches of a given tournament's match (tree node) 
}
